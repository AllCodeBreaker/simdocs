# Guidelines for contributors 
Each and every module/package are seperated by its language name inside the content directory.
It should have buildins, framework, and others directory which have module/framework name and content.
follow the [file Structure](file-structure.md) for every functions, classes, constants etc. 

